# Commit Guidelines

- Use the imperative mood and keep the subject concise (max 72 chars).
- Prefer Conventional Commits: `type(scope): subject`.
- Common types: `chore`, `docs`, `feat`, `fix`, `refactor`, `test`.
- Keep the body optional; use it only to explain the why.
- One logical change per commit.
